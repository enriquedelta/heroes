import { ImagenHeroePipe } from './imagen-heroe.pipe';

describe('ImagenHeroePipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenHeroePipe();
    expect(pipe).toBeTruthy();
  });
});
